run = "g++ -o Labs/lab3 Labs/lab3.c -lm && ./Labs/lab3 && rm Labs/lab3"

[nix]
channel = "stable-24_05"

[deployment]
run = [
  "sh",
  "-c",
  "g++ -o Labs/lab3 Labs/lab3.c -lm && ./Labs/lab3 && rm Labs/lab3",
]

# ----------------for running all files simmutaniosly-------------
# run = "for file in Labs/*.c; do g++ -o ${file%.*} $file -lm && ./${file%.*}; done"

# [nix]
# channel = "stable-24_05"

# [deployment]
# run = [
#   "sh",
#   "-c",
#   "for file in Labs/*.c; do g++ -o ${file%.*} $file -lm && ./${file%.*}; done",
# ]
